# S10 C-arm: exact-zero qg/rate denominator guard

**Status: source overlays and synthetic replay are prepared; no host compile or native run has occurred.** This branch is rebased onto current public main `6b88ec58` after PR #275. The canonical private host tree was read only. S15 owns the native slot, so the planned disposable host build and paired runs remain queued. S10 is OPEN; no policy or operational default is selected.

## One-factor contract

C starts from the existing B midpoint source. It keeps the same per-call OUT initialization, active `ProgB_param` path, and the explicitly unapproved `rho_mid=400 kg m-3` positive-trace fallback. The only physical change is at each `rate/rhox` volume quotient: bypass only when the qg value at that exact consumer and that same process-rate numerator are both exactly binary32 zero. That arm assigns a zero quotient. Every other combination executes the original division. Thus `qg=0, rate!=0` still transfers through the original division; `qg!=0, rate=0` also takes the original division and must pass the finite-positive-density check. No qg/rate/rhox clipping or blanket process gating is introduced.

The `pgmlt/rhox` consumer is after the source updates qg and temperature. The producer branch is entered for positive pre-melt qg, but complete melt can leave post-update qg exactly zero while `pgmlt` remains nonzero. C therefore preserves that division: its same-rate guard is false. `pgdep/rhox` follows the qg mass generation update; its non-density volume addends remain in the original source order. `pgevp/rhox` and `pgeml/rhox` precede their qg mass update; nonzero rate values remain on the original division arm even if qg is zero at that use.

The four guarded quotient consumer IDs are 1418 (`pgmlt/rhox`), 2824 (`pgdep/rhox`), 2915 (`pgevp/rhox`), and 2916 (`pgeml/rhox`). The `piacr`, `paacw`, `pgacr`, `pgacs`, `biacr`, `braci`, `bsacr`, `bracs`, `bgaci`, `baacw`, and `bgacr` source terms and all temperature formulas remain unguarded and source-ordered.

## Captured operands and proof

The C-only `S10ZG` rows report call key, consumer ID, bypass/divide action, qg, rate numerator, rhox, and the applied quotient. The selected columns are `(j=73,i=113/115)` for the first microphysics step; no full-domain arrays are published. `S10MASS`, `S10VOLUME`, and `S10HEAT` report the actual source operands and stores at the melt, generation/deposition, and warm-evaporation stages. The replay checks the exact-zero action predicate; it verifies every action-0 rate and quotient are zero, every action-1 divisor is finite and positive, and every nonzero rate remains on action 1. It then recomputes qg mass, brs volume, and temperature stores in source-order binary32 arithmetic. It also verifies the independent non-density volume terms survive the pgdep bypass and the latent/heat operands still produce their original temperature update.

Before any host run, synthetic tests cover `qg=0/rate=0/rhox=0`, qg zero with nonzero rate, positive qg with zero rate, invalid action mutations, non-density brs terms, latent-rate terms, and binary32/signed-zero ordering. Source generation verifies that the S10 capture base strips to the canonical source and that disabling only `KDM6_PROGB_ZERO_QG_DIV_GUARD` recovers the B midpoint source byte-for-byte. Neither check claims native execution.

## Source and build identity

The canonical source SHA-256 values are `fc0a72d33a5e61803fea56eb9118018039c6da8b5775813032b4861a2bd66eb5` (mp37) and `4f0103c8a8321b8e854d3500f4ae567755e41eb78746fac54519eca2686a6648` (mp237). Generated C overlay SHA-256 values are `c00f89eae0d17e50ba48e5102e0a18adee328149f862f15f696ecd7f6699692f` and `cd059c5e5d93874b9e0f883b6be38ffdc15740a7e292a016427ad6ff2581c7c0`, respectively. The macro-off equality checks passed for both variants. On action 1, C retains the original inline `rate/rhox` statement; the separate quotient temporary is diagnostic-only inside the capture logger, so it does not feed physical state. The source test asserts this site-by-site equality. Full preprocessed/object/executable hashes do not yet exist because no host build was run.

After S15 releases the lane, compile each isolated shadow object using the exact retained WRF pipeline and link recipe in `harness/evidence/s10_progb_policy_shadow_build_receipt_2026-09-25.json`: comment cleanup, WRF macro `cpp`, `tools/standard.exe`, final `cpp`, then `mpif90` on `.f90`, with `-ffp-contract=off`; shadow object precedes the unchanged host archive. Add `-DKDM6_PROGB_ZERO_QG_DIV_GUARD` to the B midpoint `KDM6_PROGB_VALIDITY_CAPTURE` + `KDM6_PROGB_POLICY_MIDPOINT` macros. Build artifacts remain disposable and outside the canonical host. The exact macro-off strip and source hashes must be rechecked on both variants before execution.

The run plan uses the retained LC05 input hashes from S10, unchanged 20 s timestep, one MPI rank/thread, `1x1`, and saved times 0/20 s. For each scheme, compile B and C from the same overlay with only the guard macro toggled, then run same-binary logging-off/on control/capture pairs for each arm. Within each pair require 253 numeric raw-bit-equal fields plus exact `Times` (254 variables total), valid run receipts, and complete C event replay. Compare B midpoint to C separately; no between-arm bitwise parity is expected. Preserve the prior A/B failures (`brs=-Infinity` under retention; qg=0/rhox=0 consumers under midpoint) unchanged. A positive-trace density decision, whole-domain physical budget, forecast impact, and any operational adoption remain unresolved.
